/*
import { Module } from '../Types.ts';

interface requirement {
    name: string;
    moduleCodes: string[];
    minUnits: number;
}

// Common Curriculum
const University_Level: requirement = {
    name: 'University Level',
    moduleCodes: 
};

const Computing_Ethics = {};
*/